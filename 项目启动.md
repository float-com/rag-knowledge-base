# 项目启动

本文档只说明本项目的本地启动方式，以及如何在前端验证文档预签名直传流程。

## 一、启动前准备

### 1. 检查必要软件

本地需要安装：

- Docker Desktop
- Node.js 和 npm
- Python 3.12+
- uv

确认命令是否可用：

```powershell
docker --version
node --version
npm --version
python --version
uv --version
```

### 2. 检查根目录 `.env`

项目根目录需要存在 `.env` 文件：

```text
D:\code\rag-knowledge-base\.env
```

至少需要确认以下配置已经填写：

```dotenv
DATABASE_URL=postgresql+asyncpg://rag:rag@localhost:5432/rag_kb
COS_SECRET_ID=你的腾讯云SecretId
COS_SECRET_KEY=你的腾讯云SecretKey
COS_REGION=你的COS地域
COS_BUCKET=你的COS存储桶名称
```

文档解析和向量化还需要配置：

```dotenv
EMBEDDING_API_KEY=你的向量模型API Key
EMBEDDING_BASE_URL=https://dashscope.aliyuncs.com/compatible-mode/v1
EMBEDDING_MODEL=text-embedding-v3
```

不要将 `.env`、COS 密钥或 Embedding API Key 提交到 GitHub，也不要在截图中展示真实密钥。

## 二、启动数据库

在项目根目录打开 终端：

```powershell
docker compose up -d postgres
```

检查 PostgreSQL 容器：

```powershell
docker ps
```

正常情况下可以看到容器：

```text
rag-kb-postgres
```

如果数据库没有正常启动，查看日志：

```powershell
docker logs rag-kb-postgres
```

## 三、启动后端

### 1. 安装后端依赖

打开一个新的 PowerShell：

```powershell
cd D:\code\rag-knowledge-base\backend
uv sync
```

### 2. 执行数据库迁移

首次启动，或者新增数据库迁移后执行：

```powershell
cd D:\code\rag-knowledge-base\backend
uv run alembic upgrade head
```

确认迁移版本：

```powershell
uv run alembic current
```

预签名上传功能正常运行时，数据库中应包含 `upload_sessions` 表。

### 3. 启动 FastAPI

仍然在 `backend` 目录执行：

```powershell
uv run uvicorn app.main:app --reload --host 0.0.0.0 --port 8000
```

后端启动后保持该窗口运行。

常用地址：

- 后端服务：http://localhost:8000
- Swagger：http://localhost:8000/docs
- 应用健康检查：http://localhost:8000/api/health
- 数据库健康检查：http://localhost:8000/api/health/db
- COS 健康检查：http://localhost:8000/api/health/cos

看到 `Application startup complete` 后，说明 FastAPI 已经启动。

## 四、首次解析前：预热 Docling 模型

### 1. 为什么需要这一步

Docling 解析 PDF / 图片时，会从 Hugging Face Hub 下载版面分析、表格识别等模型权重。
`huggingface_hub` 只在 **import 阶段** 读取一次 `HF_ENDPOINT`，而 pydantic-settings 读取
`.env` 后并不会把它写回进程环境变量，因此仅把 `HF_ENDPOINT` 写进 `.env` 是不会生效的。

本项目的处理方式：

- `app/core/config.py` 显式声明 `hf_endpoint` / `hf_home` / `hf_hub_offline` 三个字段
  （没有声明的键会被 `extra="ignore"` 静默丢弃，这正是此前镜像配置失效的原因）；
- `app/core/hf_env.py` 负责把它们注入 `os.environ`；
- `app/main.py` 在**所有业务导入之前**调用 `setup_hf_environment()`，确保先于 `huggingface_hub` 生效。

### 2. 确认配置

`.env` 中需要包含：

```dotenv
HF_ENDPOINT=https://hf-mirror.com
HF_HOME=D:\code\rag-knowledge-base\models
HF_HUB_OFFLINE=false
```

如果在后端启动日志中没有出现相关报错，也可以直接用一行命令确认镜像是否真的生效：

```powershell
cd D:\code\rag-knowledge-base\backend
uv run python -c "import app.main; from huggingface_hub import constants; print(constants.ENDPOINT)"
```

预期输出：

```text
https://hf-mirror.com
```

### 3. 执行预热

模型体积有数百 MB，首次下载需要几分钟：

```powershell
cd D:\code\rag-knowledge-base\backend
uv run python scripts/preheat_models.py
```

脚本特性：

- 需要哪些模型仓库是从 Docling 自身配置反查出来的，不硬编码仓库名，
  Docling 升级后无需改动脚本；
- 支持断点续传，重复执行不会重复下载；
- 只想看看会下载什么，可以先加 `--dry-run`。

正常结束时会输出：

```text
SUCCESS: all 3 repositories are ready.
```

### 4. 预热完成后切换为离线模式

模型就绪后，在 `.env` 中设置：

```dotenv
HF_HUB_OFFLINE=true
```

此后 Docling 全部从本地 `models` 目录加载，完全不依赖外网，重启后端生效。

### 5. 如果预热带不来模型

若镜像无法访问，脚本会明确指出失败原因；常见情况：

- `LocalEntryNotFoundError` 配合 `SSL: UNEXPECTED_EOF_WHILE_READING`：
  当前网络到镜像站的 TLS 连接被中断，换网络或换镜像源后重试；
- `NOT in local cache`：在 `HF_HUB_OFFLINE=true` 状态下运行，但模型还没下载过，
  先临时改为 `false` 完成预热。

## 五、启动前端

打开第二个 PowerShell：

```powershell
cd D:\code\rag-knowledge-base\frontend
npm install
npm run dev
```

前端启动后访问：

```text
http://localhost:5173
```

开发环境中，前端发送到 `/api` 的请求会由 Vite 自动转发到：

```text
http://localhost:8000
```

因此前端页面中不要把请求地址改成 COS 地址，也不要在前端配置 COS 密钥。

## 六、在前端测试预签名直传

### 1. 登录系统

打开 http://localhost:5173 。

登录页当前提供的本地默认账号是：

```text
用户名：admin
密码：admin
```

登录成功后进入文档页面：

```text
http://localhost:5173/documents
```

### 2. 上传一个正常文档

在文档页面执行：

1. 点击“上传文档”。
2. 选择一个支持的文件。
3. 支持的扩展名包括 `.pdf`、`.docx`、`.md`、`.markdown`、`.html`、`.htm`。
4. 权限标签可以先留空，留空表示公开文档。
5. 点击提交。

前端会依次执行三步：

```text
POST /api/documents/uploads/init
PUT  预签名 URL
POST /api/documents/uploads/{upload_id}/complete
```

提交成功后，页面会提示文件已上传并正在后台处理。文档列表会自动刷新，解析期间可能经历：

```text
UPLOADING -> PARSING -> INDEXING -> READY
```

最终看到文档进入完成状态，说明上传、哈希计算、建档、解析、切块和向量化流程都已完成。

### 3. 使用浏览器开发者工具确认三步请求

按 `F12` 打开开发者工具，进入 `Network` 面板，再上传一个文件。

重点确认：

| 请求 | 预期结果 |
| --- | --- |
| `POST /api/documents/uploads/init` | 返回 `upload_id`、`object_key`、`presigned_url`、`status=initiated` |
| 预签名 URL 的 `PUT` | 请求直接发往腾讯云 COS，返回 `200` 或 `204` |
| `POST /api/documents/uploads/{upload_id}/complete` | 返回 `status=finalizing` |
| `GET /api/documents` | 后续轮询可以看到文档状态变化 |

检查 COS 的 PUT 请求头：

```text
Content-Type
```

它必须与 init 阶段后端保存并签名的 MIME 类型一致。前端当前使用的是：

```text
Content-Type: initialized.mime_type
```

### 4. 测试失败情况

可以分别验证以下场景：

- 选择不支持的扩展名，确认初始化请求被拒绝。
- 选择空文件，确认初始化请求被拒绝。
- 选择超过 `UPLOAD_MAX_SIZE_MB` 的文件，确认初始化请求被拒绝。
- 在 COS PUT 失败后，确认前端不会继续调用 complete 接口。
- 上传成功后观察文档列表，确认后台状态会继续更新。
- 上传同一个文件两次，确认第二次会走 SHA-256 重复文件判断，不会重复创建相同内容的正式文档。

## 七、常见问题排查

### 1. 页面无法打开

确认前端终端仍在运行，并检查访问地址是否为：

```text
http://localhost:5173
```

### 2. 页面请求返回 502 或 Network Error

确认后端终端仍在运行，并访问：

```text
http://localhost:8000/api/health
```

如果健康检查失败，先处理后端启动错误。

### 3. 数据库健康检查失败

确认数据库容器正在运行：

```powershell
docker ps
```

再确认 `.env` 中的 `DATABASE_URL` 与 Docker Compose 的数据库用户名、密码、端口和数据库名一致，并重新执行：

```powershell
cd D:\code\rag-knowledge-base\backend
uv run alembic upgrade head
```

### 4. COS 健康检查失败或 PUT 失败

检查以下内容：

- `COS_SECRET_ID` 是否正确。
- `COS_SECRET_KEY` 是否正确。
- `COS_REGION` 是否与存储桶地域一致。
- `COS_BUCKET` 是否包含正确的存储桶名称和 APPID 后缀。
- 腾讯云 COS 存储桶是否配置了允许 `http://localhost:5173` 的跨域规则。
- 浏览器 Network 面板中的 PUT 请求 `Content-Type` 是否与签名使用的 MIME 类型一致。

### 5. 上传成功但文档一直没有完成

查看后端终端日志，重点检查：

- Embedding API Key 是否有效。
- Docling 模型是否已经预热完成（见第四节）。
- 文档格式和内容是否可以正常解析。
- PostgreSQL 和 pgvector 是否正常连接。

如果文档详情页显示 `Docling 解析失败: Got: ConnectError: [SSL: UNEXPECTED_EOF_WHILE_READING]`，
说明模型权重没有下载成功：请先按第四节完成预热，然后在文档列表对该文档点击「重试」。

上传阶段成功只代表文件已经进入 COS；后续解析和向量化由后端后台任务继续执行。

## 八、停止服务

停止前端和后端时，在对应终端按：

```text
Ctrl + C
```

停止 PostgreSQL 容器：

```powershell
cd D:\code\rag-knowledge-base
docker compose stop postgres
```

